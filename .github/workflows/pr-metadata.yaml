name: PR metadata annotation

on: 
  workflow_dispatch         
jobs:

  annotate-pr:
    runs-on: self-hosted
    name: Annotates pull request with metadata
    steps:
      - name: Annotate PR
        uses: anuraagkb/PR-metadata-action@main
        with:
          workspace: D:\HCL\hclonetest\workspace
          project: PT1
          suite: Test1
